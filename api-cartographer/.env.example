# Скопируйте файл в .env.local или задайте переменную в окружении.
# Никогда не коммитьте настоящий токен.
API_BEARER_TOKEN=replace-me
# Список origin через запятую. Токен не будет отправлен другим адресатам.
API_BEARER_ORIGINS=https://api.example.internal
